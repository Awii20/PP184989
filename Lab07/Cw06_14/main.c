#include <stdio.h>
#include <stdlib.h>

int main(){
    int a;
    printf("Wpisz liczbe naturalna: ");
    scanf("%d", &a);
    if (a=1){
        printf("Jest liczba pierwsza");
    }
    for(int i=2, i*i <= a; i++ ){

    }
}
